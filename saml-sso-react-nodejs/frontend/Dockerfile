FROM node:current-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

ARG FRONTEND_PORT=3000
ENV FRONTEND_PORT=${FRONTEND_PORT}
EXPOSE ${FRONTEND_PORT}

ENV PORT=${FRONTEND_PORT}
ENV WDS_SOCKET_PORT=${FRONTEND_PORT}

CMD ["npm", "start"]